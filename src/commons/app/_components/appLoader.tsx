import { AuthLoader } from "ebp/auth/_components/authLoader";

/**
 * AppLoader is a React component that loads the authentication and theme contexts.
 * It is used to ensure that the application has the necessary context before rendering.
 */
export const AppLoader = () => <><AuthLoader /></>