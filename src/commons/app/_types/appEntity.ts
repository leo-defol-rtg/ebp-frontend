/**
 * AppEntity interface represents a base structure for entities  in the application.
 * It includes common fields that are typically required for tracking creation
 * and modification of entities, such as a identifier, timestamps for creation and
 * updates, and the uder who created or updated the entity.
 */
export type AppEntity = object;