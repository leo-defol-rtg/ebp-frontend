/**
 * Enum representing different types of authentication actions.
 * This enum is used to defined the action types for authentication-related operations
 * such as logging in, logging out, and loading authentication state.
 */
export enum AuthActionTypeEnum {
    AUTH = 'AUTH',
    LOGOUT = 'LOGOUT',
    LOAD = 'LOAD',
}