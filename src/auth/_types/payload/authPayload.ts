/**
 * AuthPayload interface represents the structure of the authentication payload
 * used in the application. It contains a single property, token, which is a string
 * representing the authentication otken for the user.
 */
export interface AuthPayload {
    token: string;
}